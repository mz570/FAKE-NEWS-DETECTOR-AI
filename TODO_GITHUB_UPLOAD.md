# TODO: Upload whole project to GitHub

## Steps
1. Initialize git in project folder.
2. Stage all project files.
3. Create initial commit.
4. Add GitHub remote URL.
5. Push to `main`.
6. Ensure secrets are not pushed (add `.gitignore`, then remove cached secrets if needed).

